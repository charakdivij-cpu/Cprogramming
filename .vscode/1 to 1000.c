#include <stdio.h>

int main() {
    int num, rem, sum, original;

    printf("Armstrong numbers between 1 and 1000 are:\n");

    for (num = 1; num <= 1000; num++) {
        sum = 0;
        original = num;

        while (orig
